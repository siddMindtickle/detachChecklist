import { MT_MODULES } from "@config/global.config";

export const mixpanelIdentityPath = {
  moduleType: MT_MODULES.CHECKLIST,
  moduleName: "@@store.checklist.moduleData.name",
  moduleId: "@@store.checklist.moduleData.id",
  seriesId: "@@store.checklist.seriesData.id",
  seriesName: "@@store.checklist.seriesData.name"
};
